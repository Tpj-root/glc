🔧 APT-FILE HELP

## 1. Install and update:

  sudo apt update
  sudo apt install apt-file
  sudo apt-file update

---

## 2. Search for a file (e.g., a .so library):

  apt-file search libbrotlicommon.so.1

  Output:
    libbrotli1: /usr/lib/x86_64-linux-gnu/libbrotlicommon.so.1

---

## 3. Search for a header file (.h):

  apt-file search stdio.h

  Output:
    libc6-dev: /usr/include/stdio.h

---

## 4. Search for binary files:

  apt-file search bin/nano

---

## 5. Find config files:

  apt-file search etc/nginx/nginx.conf

---

## 6. List all files in a package:

  apt-file list libbrotli1

---

## 7. Search libraries:

  apt-file search /usr/lib/x86_64-linux-gnu/libm.so

---

Use this tool to find missing files, headers, libraries, or binaries!
